<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".MainMenuActivity"
    tools:showIn="@layout/activity_main_menu">

    <Button
        android:id="@+id/add_to_inventory_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/add_to_inventory"
        android:layout_centerHorizontal="true"
        android:background="@android:color/holo_green_dark"
        android:padding="40dp"
        android:layout_margin="10dp"
        android:onClick="addToInventory" />

    <Button
        android:id="@+id/remove_from_inventory_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/remove_from_inventory"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/add_to_inventory_button"
        android:background="@android:color/holo_red_light"
        android:padding="40dp"
        android:layout_margin="10dp"
        android:onClick="removeFromInventory" />

    <Button
        android:id="@+id/view_inventory_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/view_inventory"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/remove_from_inventory_button"
        android:background="@android:color/holo_orange_dark"
        android:padding="40dp"
        android:layout_margin="10dp"
        android:onClick="viewInventory" />

    <Button
        android:id="@+id/switch_inventory_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/switch_inventory"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/view_inventory_button"
        android:background="@android:color/holo_blue_dark"
        android:padding="40dp"
        android:layout_margin="10dp"
        android:onClick="switchInventory" />

</RelativeLayout>
